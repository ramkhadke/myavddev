def sum (a,b)
{
  echo "the sum of $ {a} and {b} is $ {a+b}"
}
def mul (a,b)
{
  echo "the mul of $ {a} and {b} is $ {a+b}"
}
def subt (a,b)
{
  echo "the subt of $ {a} and {b} is $ {a+b}"
}
def div (a,b)
{
  echo "the div of $ {a} and {b} is $ {a+b}"
}
